# Active profile (set via env var: SPRING_PROFILES_ACTIVE=gcp)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Server
server.port=8080

# Logging
logging.level.org.springframework.web=INFO
logging.level.jobapplier=DEBUG